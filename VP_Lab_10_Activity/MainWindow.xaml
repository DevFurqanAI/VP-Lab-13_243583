<Window x:Class="VP_Lab_10_Activity.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Student Management" Height="450" Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Input Fields -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Margin="0,0,20,0">
                <TextBlock Text="Roll No:"/>
                <TextBox x:Name="RollNoTextBox" Width="150"/>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Name:"/>
                <TextBox x:Name="NameTextBox" Width="200"/>
            </StackPanel>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <Button x:Name="InsertButton" Content="Insert" Width="100" Margin="5" Click="InsertButton_Click"/>
            <Button x:Name="UpdateButton" Content="Update" Width="100" Margin="5" Click="UpdateButton_Click"/>
            <Button x:Name="DeleteButton" Content="Delete" Width="100" Margin="5" Click="DeleteButton_Click"/>
            <Button x:Name="SearchButton" Content="Search" Width="100" Margin="5" Click="SearchButton_Click"/>
            <Button x:Name="ViewAllButton" Content="View All" Width="100" Margin="5" Click="ViewAllButton_Click"/>
        </StackPanel>

        <!-- Data Grid -->
        <DataGrid x:Name="StudentsDataGrid" Grid.Row="2" AutoGenerateColumns="True" 
                  IsReadOnly="True" Margin="0,10,0,0"/>
    </Grid>
</Window>
